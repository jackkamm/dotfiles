# -*- mode: R -*-

options(menu.graphics=FALSE)
options(width=60)

## fix ggplot2 hidpi scaling
library(ggplot2)
theme_set(theme_gray(base_size=20) + theme(legend.key.size=unit(.6, "line"),
                                           legend.box.spacing = unit(.01,"cm")))
update_geom_defaults("text", list(size=6))

